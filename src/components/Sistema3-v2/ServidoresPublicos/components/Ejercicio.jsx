import React from 'react';
import { Paper, Typography } from '@mui/material';

const Ejercicio = () => {
  return (
    <Paper elevation={0} sx={{ p: 2, height: '100%' }}>
      <Typography variant="h6">Ejercicio</Typography>
    </Paper>
  );
};

export default Ejercicio;