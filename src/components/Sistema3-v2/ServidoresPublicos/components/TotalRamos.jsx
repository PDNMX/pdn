import React from 'react';
import { Paper, Typography } from '@mui/material';

const TotalRamos = () => {
  return (
    <Paper elevation={0} sx={{ p: 2, height: '100%' }}>
      <Typography variant="h6">Total de Ramos</Typography>
    </Paper>
  );
};

export default TotalRamos;